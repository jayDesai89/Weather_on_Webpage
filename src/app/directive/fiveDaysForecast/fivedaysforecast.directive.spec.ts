import { FivedaysforecastDirective } from './fivedaysforecast.directive';

describe('FivedaysforecastDirective', () => {
  it('should create an instance', () => {
    const directive = new FivedaysforecastDirective();
    expect(directive).toBeTruthy();
  });
});
