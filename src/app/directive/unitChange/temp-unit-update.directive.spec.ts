import { TempUnitUpdateDirective } from './temp-unit-update.directive';

describe('TempUnitUpdateDirective', () => {
  it('should create an instance', () => {
    const directive = new TempUnitUpdateDirective();
    expect(directive).toBeTruthy();
  });
});
