import { CheckDatePipe } from './check-date.pipe';

describe('CheckDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDatePipe();
    expect(pipe).toBeTruthy();
  });
});
